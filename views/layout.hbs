<!DOCTYPE html>
<html>
    <head>
        <title>{{title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <h1>{{title}}</h1>
        <nav>
            <a href="/">home</a>
            {{#if thisUser.username}}
                <a href="/settings">settings</a>
                <a href="/logout">logout</a>
            {{else}}
                <a href="/login">login</a>
                <a href="/register">register</a>
            {{/if}}
        </nav>
        <div>
            {{#if thisUser.username}}
                Hello, {{{thisUser.username}}}
                {{else}}
                Not logged in.
            {{/if}}
        </div>
        {{{body}}}
    </body>
</html>
